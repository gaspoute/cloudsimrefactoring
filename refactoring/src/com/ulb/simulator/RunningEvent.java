package com.ulb.simulator;

import com.ulb.utility.Event;

public class RunningEvent extends Event {

    public RunningEvent(Object source, Object target, long when) {
        super(source, target, when);
    }
}
